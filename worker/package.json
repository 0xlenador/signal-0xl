{
  "name": "signal0xl-ranking",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:cron": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute RANKING_DB --local --file=./schema.sql",
    "db:deploy": "wrangler d1 execute RANKING_DB --remote --file=./schema.sql"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "drizzle-orm": "^0.31.2",
    "hono": "^4.5.0",
    "viem": "^2.17.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "drizzle-kit": "^0.22.8",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
